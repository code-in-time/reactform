// import { Routes, Route, Link } from "react-router-dom";

function TextMobx(): JSX.Element {

  // const newCount = () => {
  //   let newCount = mainCounter + 1; 
  //   setMainCounter(newCount); 
  //   console.log();
  // }

  return (
    <div className="TextMobx">
      {/* Home maincounter = {mainCounter}
      <br /><br />
      <button type="button" class="btn btn-primary" onClick={() => newCount()}>add button</button> */}
    </div>
  );
}

export default TextMobx;
